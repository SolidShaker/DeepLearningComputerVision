# Lab 02: k-NN Distance Computation

Open `knn.ipynb` and complete the three distance functions in `knn.py`.

Learning goals:

- compute squared Euclidean distances;
- move from two loops to one loop and then no Python loops;
- verify correctness with independent tests;
- compare runtime and explain the benefit of vectorization.
